dart_defines.env
dart_defines.json
lib/core/services/firebase_config.dart
google-services.json

# Android — machine-specific, never commit these
android/local.properties
android/key.properties
android/*.jks
android/*.keystore
android/app/*.jks
android/app/*.keystore




# Gradle build cache — generated locally, never commit
android/.gradle/
.gradle/
build/
android/build/
